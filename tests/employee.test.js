const { italic } = require('chalk');
const { clear } = require('console');
const Employee = require('../lib/Employee');

describe('Employee', () => {
    it("should creat a new employee instance", () => {
        const judith = new Employee(judith)
    });
});